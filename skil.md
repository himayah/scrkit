## **AI Coding Skill: Structured Software Development Workflow with Mode Assignment**

### **目的**
AI を用いたソフトウェア開発を、  
**要件定義 → 設計 → 実装 → テスト → レビュー → ドキュメント化 → githubへのpush**  
まで一貫して行うための標準プロセスを定義し、  
実行モードを **plan / auto** に明確に指定することで、再現性の高い開発を実現する。
モデルはdefaultのモデルを使用する。

---

## **モードの割り当て（必ず遵守する）**

### **plan モードを使用する工程**
1. **要件の受領と構造化**  
2. **設計書の作成**  
3. **第三者視点での設計レビュー**

### **auto モードを使用する工程**
4. **実装とテストコード作成**  
5. **第三者視点でのコードレビュー**  
6. **要件との整合性チェック**  
7. **README.md（GitHub 公開用）の作成**
8. **githubへpush**

---

## **開発プロセス（順番通りに実行する）**

### **1. 要件の受領と構造化（plan）**
- 要件ファイル（例：要件.txt）を読み取り、内容を構造化する。  
- 不明点・曖昧な点があれば質問する。  
- 要件を **機能要件 / 非機能要件 / 制約条件 / 入出力仕様 / 想定ユーザー / 実行環境** に分類して整理する。

---

### **2. 設計書の作成（plan）**
以下を含む詳細な設計書を作成する：

- 全体アーキテクチャ  
- モジュール構成  
- データ構造  
- API 仕様（必要な場合）  
- エラーハンドリング方針  
- ロギング方針  
- テスト方針（単体・結合）  

設計書は第三者が読んでも実装可能なレベルまで詳細化する。

---

### **3. 第三者視点での設計レビュー（plan）**
- 設計書を「自分が書いていない前提」でレビューする。  
- 問題点・曖昧さ・冗長さ・要件との不整合を指摘する。  
- コメントは **重大度（High / Medium / Low）** を付けて箇条書きで提示する。  
- 必要に応じて設計書を修正する。

---

### **4. 実装とテストコード作成（auto）**
- 修正済み設計書に基づきコードを実装する。  
- 可能な限り **小さな関数・小さなモジュール** に分割する。  
- テストコード（ユニットテスト）を必ず作成する。  
- 言語ごとに標準的なテストフレームワークを優先する：  
  - Python → pytest  
  - Node.js → jest / vitest  
  - Go → testing  
  - Rust → cargo test  
- テストを実行し、成功するまで修正する。

---

### **5. 第三者視点でのコードレビュー（auto）**
- 完成したコードを「経験豊富な別のエンジニア視点」でレビューする。  
- 可読性、保守性、命名、責務分離、例外処理、テスト網羅性をチェックする。  
- コメントは **重大度（High / Medium / Low）** を付けて箇条書きで提示する。  
- 問題点があれば修正し、テストを再実行する。

---

### **6. 要件との整合性チェック（auto）**
- 最初の要件と完成したコードを照合する。  
- 要件漏れ・要件違反・仕様逸脱がないか確認する。  
- 必要に応じてコードとテストを修正する。

---

### **7. README.md の作成（auto）**
GitHub 公開用に以下を必ず含める：

- ソフト概要  
- 技術的特徴  
- 依存ライブラリとインストール手順  
- 起動方法  
- 使用方法（CLI ならコマンド例）  
- 終了方法  
- 使用上の注意点  
- コードとドキュメントはAIコーディングで作成されたことを明示  
- ライセンス（Apache License 2.0）  

---

### **8. githubへpush（auto）**
第三者が自分の環境にダウンロードして実行するのに必要なコード、ドキュメントを選別し、commit、pushする

- push先のアカウントは@himayah  


図表にはmermaidを利用する。
README は第三者が迷わず使える品質で作成する。

---

## **スタイル規範**
- CLI・再現性重視のユーザー向けに曖昧な表現を避ける。  
- コードは必ず動作する最小単位まで分割する。  
- Windows / WSL2 / Linux の環境差異に注意する。  
- コマンド例は **コピペで動く形** にする。  
- 日本語で記述し、技術用語は英語併記する。  
- 長文生成は **500〜800 行を超える場合、自動的に分割して段階的に作成する**。  
- 複数ファイルの生成は **1 ファイルずつ順番に行う**。  
- 開発言語・仕様ライブラリの選定は、要件に反しない限り **テストカバレッジを最大化できる選択を優先する**。

---

## **レビュー基準**
- 第三者視点を徹底する。  
- 要件との整合性を最終チェックで必ず行う。  
- テストが通ることを完成条件とする。

---

## **禁止事項**
- 要件を勝手に拡張しない。  
- 設計書とコードの差異を放置しない。  
- コードとドキュメントの差異を放置しない。
- テストなしで実装を完了したとみなさない。

---

## **最終成果物**
- 設計書（Markdown）  
- 実装コード  
- テストコード  
- README.md  
- 必要なら補助スクリプト（install.sh など）
