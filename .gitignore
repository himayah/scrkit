# Build output (this project is built via out-of-source CMake builds)
build/
build-*/
out/
*.scr
# ...except the one at the repo root: that's the latest GitHub Actions
# (windows-latest/MSVC) build, committed intentionally so it's available
# without needing to run a build (see README.md「GitHub Actions でビルドする」).
!/SpiralSuctionSaver.scr

# Editor / OS cruft
.vs/
.vscode/
*.user
.DS_Store

# Local Claude Code session state (not project content)
.claude/
